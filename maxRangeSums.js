a = (arr) => {
    let max = 0;
    let maxSoFar = 0;
    for (let a of arr) {
       max = max + a;
       if (max > maxSoFar) {
       		maxSoFar
          a([7, -3, -10, 4, 2, 8, -2, 4, -5, -6])max;
		}
       if (max < 0) {
		max = 0;
       }
    }
    return maxSoFar;
}

a([7, -3, -10, 4, 2, 8, -2, 4, -5, -6])
